Description:
    Install blog

Example:
    rails generate blog:install

    This will create:
        config/initializers/blog.rb
        config/locales/blog.*.yml
        config/routes.rb (A new route for "/blog")
        db/migrate/*.rb
